# Аналитические Эндпоинты - Краткое Руководство

## Что было добавлено

Реализованы 5 аналитических эндпоинтов для получения статистики проекта:

### 📊 Эндпоинты

| Маршрут | Метод | Описание |
|---------|-------|---------|
| `/analytics/summary` | GET | Полная сводка по проекту |
| `/analytics/stats` | GET | Статистика БД и хранилища |
| `/analytics/usage` | GET | Частота использования по времени |
| `/analytics/authors` | GET | Статистика по авторам |
| `/analytics/timeline` | GET | Временная линия (30 дней) |

---

## 🚀 Быстрый старт

### Запуск приложения

```bash
npm install
npm start
```

Приложение будет доступно на `http://localhost:3000`

### Получение аналитики

#### 1. Полная сводка
```bash
curl http://localhost:3000/analytics/summary
```

#### 2. Базовая статистика
```bash
curl http://localhost:3000/analytics/stats
```

#### 3. Использование по дням
```bash
curl http://localhost:3000/analytics/usage
```

#### 4. Использование по месяцам
```bash
curl http://localhost:3000/analytics/usage?period=monthly
```

#### 5. Статистика авторов
```bash
curl http://localhost:3000/analytics/authors
```

#### 6. Временная линия
```bash
curl http://localhost:3000/analytics/timeline
```

---

## 📁 Файлы

### Новые файлы
- **`utils/analytics.js`** - модуль с функциями аналитики
- **`ANALYTICS.md`** - подробная документация по API

### Модифицированные файлы
- **`routes/index.js`** - добавлены аналитические маршруты

---

## 📊 Примеры результатов

### Summary (сводка)
```json
{
  "summary": {
    "total_images": 150,
    "unique_authors": 25,
    "total_storage_mb": "542.30",
    "average_daily_uploads": "3.50"
  },
  "top_authors": [...],
  "storage": {...}
}
```

### Usage (использование)
```json
{
  "period": "daily",
  "data": [
    {
      "time_period": "2024-12-01",
      "uploads_count": 5,
      "unique_authors": 3
    }
  ]
}
```

### Authors (авторы)
```json
{
  "data": [
    {
      "author": "John Doe",
      "images_count": 32,
      "first_upload": "2024-01-15T10:30:00.000Z",
      "last_upload": "2024-12-28T15:20:00.000Z"
    }
  ]
}
```

---

## 🔍 Что отслеживается

✅ Общее количество изображений  
✅ Уникальные авторы  
✅ Объем занимаемого хранилища  
✅ Средняя длина имён и описаний  
✅ Частота загрузок по периодам (часы, дни, недели, месяцы)  
✅ Статистика по авторам  
✅ Диапазон дат (первое и последнее изображение)  
✅ Средний размер файла  

---

## 💡 Советы по использованию

### В браузере
Посетите `http://localhost:3000/analytics/summary` чтобы увидеть JSON в браузере

### С JavaScript
```javascript
const response = await fetch('/analytics/summary');
const data = await response.json();
console.log(data.data.summary);
```

### С инструментами визуализации
Используйте результаты из `/analytics/usage` для построения графиков в вашем frontend приложении

---

## 🧪 Тестирование

Все эндпоинты протестированы на синтаксис и готовы к использованию.

Для полного функционирования убедитесь, что:
1. База данных инициализирована
2. Есть данные в таблице `data`
3. Изображения сохранены в `public/images/`

---

## 📚 Подробная документация

Для более детальной информации смотрите [ANALYTICS.md](./ANALYTICS.md)
